def U(A):return max(A for(A,B)in J(A))
def E(A):return min(A for(A,B)in J(A))
def H(A):return frozenset({(A[0]-1,A[1]-1),(A[0]-1,A[1]+1),(A[0]+1,A[1]-1),(A[0]+1,A[1]+1)})
def M(A):return G(A)|H(A)
def G(A):return frozenset({(A[0]-1,A[1]),(A[0]+1,A[1]),(A[0],A[1]-1),(A[0],A[1]+1)})
def X(A):return frozenset((B,C)for B in range(len(A))for C in range(len(A[0])))
def R(A):B=[B for A in A for B in A]if isinstance(A,tuple)else[A for(A,B)in A];return max(set(B),key=B.count)
def V(A):return max(A for(B,A)in J(A))
def K(A):return min(A for(B,A)in J(A))
def GG(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A[0])
	return V(A)-K(A)+1
def GH(A,B):
	C,D=B;E,F=len(A),len(A[0])
	if not(0<=C<E and 0<=D<F):return
	return A[B[0]][B[1]]
def J(A):
	if len(A)==0:return frozenset()
	if isinstance(next(iter(A))[1],tuple):return frozenset(A for(B,A)in A)
	return A
def GJ(A,B,C):
	G,H=len(A),len(A[0]);D=list(list(A)for A in A)
	for(E,F)in J(C):
		if 0<=E<G and 0<=F<H:D[E][F]=B
	return tuple(tuple(A)for A in D)
def L(A):return len(A)==len(A[0])if isinstance(A,tuple)else W(A)*GG(A)==len(A)and W(A)==GG(A)
def P(A,B,C,D):
	L=R(A)if D else None;N=set();H=set();Q,S=len(A),len(A[0]);T=X(A);U=M if C else G
	for E in T:
		if E in H:continue
		I=A[E[0]][E[1]]
		if I==L:continue
		O={(I,E)};J={E}
		while len(J)>0:
			P=set()
			for F in J:
				K=A[F[0]][F[1]]
				if I==K if B else K!=L:O.add((K,F));H.add(F);P|={(A,B)for(A,B)in U(F)if 0<=A<Q and 0<=B<S}
			J=P-H
		N.add(frozenset(O))
	return frozenset(N)
def W(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A)
	return U(A)-E(A)+1
def S(A,B):return lambda x:A(B(x))
def Q(A,B):return type(A)(A for A in A if B(A))
def Z(A):return type(A)(B for A in A for B in A)
def Y(a,b):return type(a)(A for A in a if A not in b)
def GY(n):return n%2==0
def p(I):C=False;I=tuple(map(tuple,I));D=P(I,True,C,C);A=Q(D,L);E=S(GY,W);B=Q(A,E);F=Y(A,B);G=Z(B);H=Z(F);J=GJ(I,2,G);K=GJ(J,7,H);return[*map(list,K)]