def K(A):return max(A for(A,B)in V(A))
def R(A):return min(A for(A,B)in V(A))
def GV(A,B):
	C,D=B;E,F=len(A),len(A[0])
	if not(0<=C<E and 0<=D<F):return
	return A[B[0]][B[1]]
def V(A):
	if len(A)==0:return frozenset()
	if isinstance(next(iter(A))[1],tuple):return frozenset(A for(B,A)in A)
	return A
def M(A):return max(A for(B,A)in V(A))
def P(A):return min(A for(B,A)in V(A))
def J(A):return frozenset({(A[0]-1,A[1]-1),(A[0]-1,A[1]+1),(A[0]+1,A[1]-1),(A[0]+1,A[1]+1)})
def E(A):return H(A)|J(A)
def H(A):return frozenset({(A[0]-1,A[1]),(A[0]+1,A[1]),(A[0],A[1]-1),(A[0],A[1]+1)})
def U(A):return frozenset((B,C)for B in range(len(A))for C in range(len(A[0])))
def GH(A,B):return tuple(tuple(A for B in range(B[1]))for C in range(B[0]))
def W(a,b):return tuple(A+B for(A,B)in zip(a,b))
def GG(A,B,C,D):
	M=X(A)if D else None;N=set();I=set();Q,R=len(A),len(A[0]);S=U(A);T=E if C else H
	for F in S:
		if F in I:continue
		J=A[F[0]][F[1]]
		if J==M:continue
		O={(J,F)};K={F}
		while len(K)>0:
			P=set()
			for G in K:
				L=A[G[0]][G[1]]
				if J==L if B else L!=M:O.add((L,G));I.add(G);P|={(A,B)for(A,B)in T(G)if 0<=A<Q and 0<=B<R}
			K=P-I
		N.add(frozenset(O))
	return frozenset(N)
def Y(A,n):return frozenset(A for A in A if len(A)==n)
def G(A,B):return frozenset(A for A in A if next(iter(A))[0]==B)
def GJ(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A[0])
	return M(A)-P(A)+1
def GY(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A)
	return K(A)-R(A)+1
def X(A):B=[B for A in A for B in A]if isinstance(A,tuple)else[A for(A,B)in A];return max(set(B),key=B.count)
def GM(A,a,b):return lambda x:A(a(x),b(x))
def Z(a,b):return a,b
def L(A,B):return type(A)(A for A in A if B(A))
def GE(A):return len(A)
def S(a,b):return a==b
def Q(a,b):
	if isinstance(a,int)and isinstance(b,int):return a-b
	elif isinstance(a,tuple)and isinstance(b,tuple):return a[0]-b[0],a[1]-b[1]
	elif isinstance(a,int)and isinstance(b,tuple):return a-b[0],a-b[1]
	return a[0]-b,a[1]-b
def p(I):I=tuple(map(tuple,I));B=GG(I,True,False,True);C=G(B,1);D=Y(C,4);E=GM(S,GY,GJ);F=L(D,E);A=GE(F);H=Q(5,A);J=Z(1,A);K=GH(1,J);M=Z(1,H);N=X(I);O=GH(N,M);P=W(K,O);return[*map(list,P)]