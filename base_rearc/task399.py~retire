def K(A):return max(A for(A,B)in V(A))
def R(A):return min(A for(A,B)in V(A))
def M(A):return max(A for(B,A)in V(A))
def P(A):return min(A for(B,A)in V(A))
def GU(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A[0])
	return M(A)-P(A)+1
def GE(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A)
	return K(A)-R(A)+1
def Y(A):return frozenset({(A[0]-1,A[1]-1),(A[0]-1,A[1]+1),(A[0]+1,A[1]-1),(A[0]+1,A[1]+1)})
def E(A):return H(A)|Y(A)
def H(A):return frozenset({(A[0]-1,A[1]),(A[0]+1,A[1]),(A[0],A[1]-1),(A[0],A[1]+1)})
def U(A):return frozenset((B,C)for B in range(len(A))for C in range(len(A[0])))
def X(A):B=[B for A in A for B in A]if isinstance(A,tuple)else[A for(A,B)in A];return max(set(B),key=B.count)
def GP(A,B):
	C,D=B;E,F=len(A),len(A[0])
	if not(0<=C<E and 0<=D<F):return
	return A[B[0]][B[1]]
def V(A):
	if len(A)==0:return frozenset()
	if isinstance(next(iter(A))[1],tuple):return frozenset(A for(B,A)in A)
	return A
def GW(A,B,C):return tuple(A[B[1]:B[1]+C[1]]for A in A[B[0]:B[0]+C[0]])
def GY(A,B):return tuple(tuple(A for B in range(B[1]))for C in range(B[0]))
def GV(A,n):D,B=len(A),len(A[0])//n;E=len(A[0])%n!=0;return tuple(GW(A,(0,B*C+C*E),(D,B))for C in range(n))
def S(a,b):return tuple(A+B for(A,B)in zip(a,b))
def GS(A,B,C):
	G,H=len(A),len(A[0]);D=list(list(A)for A in A)
	for(E,F)in V(C):
		if 0<=E<G and 0<=F<H:D[E][F]=B
	return tuple(tuple(A)for A in D)
def L(A):
	if isinstance(A,tuple):return frozenset({B for A in A for B in A})
	return frozenset({A for(A,B)in A})
def Z(A,B,C,D):
	M=X(A)if D else None;N=set();I=set();Q,R=len(A),len(A[0]);S=U(A);T=E if C else H
	for F in S:
		if F in I:continue
		J=A[F[0]][F[1]]
		if J==M:continue
		O={(J,F)};K={F}
		while len(K)>0:
			P=set()
			for G in K:
				L=A[G[0]][G[1]]
				if J==L if B else L!=M:O.add((L,G));I.add(G);P|={(A,B)for(A,B)in T(G)if 0<=A<Q and 0<=B<R}
			K=P-I
		N.add(frozenset(O))
	return frozenset(N)
def G(A,B):return frozenset(A for A in A if next(iter(A))[0]==B)
def J(A,B):
	if isinstance(A,tuple):return sum(A.count(B)for A in A)
	return sum(A==B for(A,C)in A)
def GK(A):return GE(A),GU(A)
def GQ(A,B):return type(B)(A(B)for B in B)
def GR(A,B):
	C=A.__code__.co_argcount
	if C==2:return lambda y:A(B,y)
	elif C==3:return lambda y,z:A(B,y,z)
	else:return lambda y,z,a:A(B,y,z,a)
def W(a,b):return a,b
def Q(A,B,C):return tuple(range(A,B,C))
def GH(A,B):return type(B)(B for B in B if B!=A)
def GJ(j):return 0,j
def GG(A,B):return max(A,key=B)
def GX(A):return type(A)(B for A in A for B in A)
def GL(A):return len(A)
def GM(n):return n*2 if isinstance(n,int)else(n[0]*2,n[1]*2)
def p(I):I=tuple(map(tuple,I));B=L(I);C=GH(2,B);D=GR(J,I);A=GG(C,D);E=GK(I);F=GY(A,E);H=S(I,F);K=Z(H,True,False,True);M=G(K,2);N=GL(M);O=GM(N);P=Q(0,O,2);R=GQ(GJ,P);T=W(1,9);U=GY(A,T);V=GS(U,1,R);X=GV(V,3);Y=GX(X);return[*map(list,Y)]