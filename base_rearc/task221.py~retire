def P(A,B):return type(B)(B for B in B if B!=A)
def GH(A):return next(iter(A))
def V(A):return max(A for(A,B)in H(A))
def J(A):return min(A for(A,B)in H(A))
def Y(A):return max(A for(B,A)in H(A))
def X(A):return min(A for(B,A)in H(A))
def GY(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A[0])
	return Y(A)-X(A)+1
def Z(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A)
	return V(A)-J(A)+1
def GM(A,B):
	C,D=B;E,F=len(A),len(A[0])
	if not(0<=C<E and 0<=D<F):return
	return A[B[0]][B[1]]
def H(A):
	if len(A)==0:return frozenset()
	if isinstance(next(iter(A))[1],tuple):return frozenset(A for(B,A)in A)
	return A
def R(A):return tuple(map(min,zip(*H(A))))
def GE(A,B,C):return tuple(A[B[1]:B[1]+C[1]]for A in A[B[0]:B[0]+C[0]])
def L(A,B):return tuple(tuple(A for B in range(B[1]))for C in range(B[0]))
def W(A,n):D,B=len(A),len(A[0])//n;E=len(A[0])%n!=0;return tuple(GE(A,(0,B*C+C*E),(D,B))for C in range(n))
def Q(A):
	if isinstance(A,tuple):return tuple(zip(*A))
	B,C=R(A)
	if isinstance(next(iter(A))[1],tuple):return frozenset((A,(E-C+B,D-B+C))for(A,(D,E))in A)
	return frozenset((D-C+B,A-B+C)for(A,D)in A)
def K(A):
	if isinstance(A,tuple):return frozenset({B for A in A for B in A})
	return frozenset({A for(A,B)in A})
def G(A,B):
	if isinstance(A,tuple):return sum(A.count(B)for A in A)
	return sum(A==B for(A,C)in A)
def GV(A):return Z(A),GY(A)
def GJ(A,B):return GH(P(B,A))
def GG(A):return type(A)(B for A in A for B in A)
def S(A,B):return tuple(A for B in range(B))
def U(a,b):return type(a)((*a,*b))
def M(a,b):
	if isinstance(a,int)and isinstance(b,int):return a*b
	elif isinstance(a,tuple)and isinstance(b,tuple):return a[0]*b[0],a[1]*b[1]
	elif isinstance(a,int)and isinstance(b,tuple):return a*b[0],a*b[1]
	return a[0]*b,a[1]*b
def E(a,b):
	if isinstance(a,int)and isinstance(b,int):return a-b
	elif isinstance(a,tuple)and isinstance(b,tuple):return a[0]-b[0],a[1]-b[1]
	elif isinstance(a,int)and isinstance(b,tuple):return a-b[0],a-b[1]
	return a[0]-b,a[1]-b
def p(I):I=tuple(map(tuple,I));C=K(I);D=GJ(C,0);B=G(I,D);A=G(I,0);F=Q(I);H=S(F,B);J=Q(I);N=GV(J);O=L(0,N);P=M(A,A);R=E(P,B);T=S(O,R);V=U(H,T);X=GG(V);Y=Q(X);Z=W(Y,A);a=GG(Z);return[*map(list,a)]