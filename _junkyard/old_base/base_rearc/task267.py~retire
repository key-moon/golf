def W(A):return next(iter(A))
def V(A):return max(A for(A,B)in H(A))
def J(A):return min(A for(A,B)in H(A))
def GH(A,B):
	C,D=B;E,F=len(A),len(A[0])
	if not(0<=C<E and 0<=D<F):return
	return A[B[0]][B[1]]
def H(A):
	if len(A)==0:return frozenset()
	if isinstance(next(iter(A))[1],tuple):return frozenset(A for(B,A)in A)
	return A
def Y(A):return max(A for(B,A)in H(A))
def E(A):return min(A for(B,A)in H(A))
def R(A,B,C):return tuple(tuple(C if A==B else A for A in A)for A in A)
def X(A):
	if isinstance(A,tuple):return frozenset({B for A in A for B in A})
	return frozenset({A for(A,B)in A})
def U(A,B):return frozenset((A,D)for(A,C)in enumerate(A)for(D,E)in enumerate(C)if E==B)
def Z(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A[0])
	return Y(A)-E(A)+1
def S(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A)
	return V(A)-J(A)+1
def G(A):B=[B for A in A for B in A]if isinstance(A,tuple)else[A for(A,B)in A];return min(set(B),key=B.count)
def GY(A,a,b):return lambda x:A(a(x),b(x))
def L(A,B):
	C=A.__code__.co_argcount
	if C==2:return lambda y:A(B,y)
	elif C==3:return lambda y,z:A(B,y,z)
	else:return lambda y,z,a:A(B,y,z,a)
def K(A,B):return lambda x:A(B(x))
def GG(A,B):return W(P(B,A))
def P(A,B):return type(B)(B for B in B if B!=A)
def Q(A,B):return max(A,key=B)
def M(a,b):
	if isinstance(a,int)and isinstance(b,int):return a*b
	elif isinstance(a,tuple)and isinstance(b,tuple):return a[0]*b[0],a[1]*b[1]
	elif isinstance(a,int)and isinstance(b,tuple):return a*b[0],a*b[1]
	return a[0]*b,a[1]*b
def p(I):I=tuple(map(tuple,I));C=GY(M,S,Z);D=L(U,I);E=X(I);F=K(C,D);B=Q(E,F);A=G(I);H=X(I);J=P(B,H);N=GG(J,A);O=R(I,A,B);T=R(O,N,A);return[*map(list,T)]