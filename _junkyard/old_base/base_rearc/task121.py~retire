def U(A):return max(A for(A,B)in Y(A))
def J(A):return max(A for(B,A)in Y(A))
def GG(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A[0])
	return J(A)-Q(A)+1
def Z(A):
	if len(A)==0:return 0
	if isinstance(A,tuple):return len(A)
	return U(A)-M(A)+1
def Q(A):return min(A for(B,A)in Y(A))
def M(A):return min(A for(A,B)in Y(A))
def GH(A,C):
	if len(A)==0:return A
	B,D=C
	if isinstance(next(iter(A))[1],tuple):return frozenset((A,(C+B,E+D))for(A,(C,E))in A)
	return frozenset((A+B,C+D)for(A,C)in A)
def H(A):return frozenset({(A[0]-1,A[1]-1),(A[0]-1,A[1]+1),(A[0]+1,A[1]-1),(A[0]+1,A[1]+1)})
def V(A):return G(A)|H(A)
def G(A):return frozenset({(A[0]-1,A[1]),(A[0]+1,A[1]),(A[0],A[1]-1),(A[0],A[1]+1)})
def X(A):return frozenset((B,C)for B in range(len(A))for C in range(len(A[0])))
def P(A):
	if isinstance(A,tuple):return frozenset({B for A in A for B in A})
	return frozenset({A for(A,B)in A})
def GJ(A,B):
	C,D=B;E,F=len(A),len(A[0])
	if not(0<=C<E and 0<=D<F):return
	return A[B[0]][B[1]]
def Y(A):
	if len(A)==0:return frozenset()
	if isinstance(next(iter(A))[1],tuple):return frozenset(A for(B,A)in A)
	return A
def W(A,B):return tuple(tuple(A for B in range(B[1]))for C in range(B[0]))
def GV(A,B,C):
	G,H=len(A),len(A[0]);D=list(list(A)for A in A)
	for(E,F)in Y(C):
		if 0<=E<G and 0<=F<H:D[E][F]=B
	return tuple(tuple(A)for A in D)
def K(A):return len(P(A))
def S(A,B,C,D):
	L=R(A)if D else None;M=set();H=set();P,Q=len(A),len(A[0]);S=X(A);T=V if C else G
	for E in S:
		if E in H:continue
		I=A[E[0]][E[1]]
		if I==L:continue
		N={(I,E)};J={E}
		while len(J)>0:
			O=set()
			for F in J:
				K=A[F[0]][F[1]]
				if I==K if B else K!=L:N.add((K,F));H.add(F);O|={(A,B)for(A,B)in T(F)if 0<=A<P and 0<=B<Q}
			J=O-H
		M.add(frozenset(N))
	return frozenset(M)
def E(A):
	if len(A)==0:return A
	return GH(A,(-M(A),-Q(A)))
def GY(A):return Z(A),GG(A)
def R(A):B=[B for A in A for B in A]if isinstance(A,tuple)else[A for(A,B)in A];return max(set(B),key=B.count)
def L(A,B):return max(A,key=B)
def p(I):I=tuple(map(tuple,I));B=S(I,False,True,True);A=L(B,K);C=R(A);D=E(A);F=R(I);G=GY(A);H=W(F,G);J=GV(H,C,D);return[*map(list,J)]