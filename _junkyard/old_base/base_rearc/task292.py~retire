def Q(A,B):
	C,D=B;E,F=len(A),len(A[0])
	if not(0<=C<E and 0<=D<F):return
	return A[B[0]][B[1]]
def L(A,B,C):
	H,I=len(A),len(A[0]);D=list(list(A)for A in A)
	for(E,F)in G(C):
		if 0<=E<H and 0<=F<I:D[E][F]=B
	return tuple(tuple(A)for A in D)
def Y(A):return frozenset((D,(A,C))for(A,B)in enumerate(A)for(C,D)in enumerate(B))
def G(A):
	if len(A)==0:return frozenset()
	if isinstance(next(iter(A))[1],tuple):return frozenset(A for(B,A)in A)
	return A
def W(A,a,b):return lambda x:A(a(x),b(x))
def K(A,B):
	C=A.__code__.co_argcount
	if C==2:return lambda x:A(x,B)
	elif C==3:return lambda x,y:A(x,y,B)
	else:return lambda x,y,z:A(x,y,z,B)
def R(A,B):return lambda x:A(x)==B
def E(A,B):return lambda x:A(B(x))
def S(A):return max(enumerate(A))[1]
def U(A):return next(iter(A))
def M(A,B):return type(A)(A for A in A if B(A))
def V(a,b):return a==b
def P(b):return not b
def X(a,b):
	if isinstance(a,int)and isinstance(b,int):return a//b
	elif isinstance(a,tuple)and isinstance(b,tuple):return a[0]//b[0],a[1]//b[1]
	elif isinstance(a,int)and isinstance(b,tuple):return a//b[0],a//b[1]
	return a[0]//b,a[1]//b
def H(a,b):
	if isinstance(a,int)and isinstance(b,int):return a*b
	elif isinstance(a,tuple)and isinstance(b,tuple):return a[0]*b[0],a[1]*b[1]
	elif isinstance(a,int)and isinstance(b,tuple):return a*b[0],a*b[1]
	return a[0]*b,a[1]*b
def J(x):return x
def p(I):I=tuple(map(tuple,I));A=Y(I);B=R(U,0);C=E(P,B);D=M(A,C);F=K(H,3);N=K(X,3);O=E(F,N);Q=W(V,J,O);T=G(D);Z=E(Q,S);a=M(T,Z);b=L(I,6,a);return[*map(list,b)]