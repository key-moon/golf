def solve_c444b776(I):
	E,F=len(I),len(I[0]);B=[list(A)for A in I];Q=[A for A in range(E)if B[A][0]!=0 and all(B[A][C]==B[A][0]for C in range(F))];R=[A for A in range(F)if B[0][A]!=0 and all(B[C][A]==B[0][A]for C in range(E))]
	if not Q and not R:return I
	C=[];A=0
	for S in Q:
		if A<S:C.append((A,S))
		A=S+1
	if A<E:C.append((A,E))
	if not C:C=[(0,E)]
	D=[];A=0
	for T in R:
		if A<T:D.append((A,T))
		A=T+1
	if A<F:D.append((A,F))
	if not D:D=[(0,F)]
	def U(segs,x):
		for(A,(B,C))in enumerate(segs):
			if B<=x<C:return A
		return-1
	V=[]
	for J in range(E):
		for K in range(F):
			L=B[J][K]
			if L==0 or J in Q or K in R:continue
			G=U(C,J);H=U(D,K)
			if G==-1 or H==-1:continue
			M=J-C[G][0];N=K-D[H][0];V.append((G,H,M,N,L))
	for(G,H,M,N,L)in V:
		for(W,Y)in C:
			O=W+M
			if not W<=O<Y:continue
			for(X,Z)in D:
				P=X+N
				if not X<=P<Z:continue
				if O==C[G][0]+M and P==D[H][0]+N:continue
				if B[O][P]==0:B[O][P]=L
	return tuple(tuple(A)for A in B)
def p(g):A=tuple(tuple(A)for A in g);B=solve_c444b776(A);return[list(A)for A in B]