def p(j):
	B=[A[:]for A in j];E,D=len(j),len(j[0])
	for A in range(D):
		if j[0][A]:
			for C in range(E):
				if C%2==0:B[C][A]=j[0][A]
				else:
					if A>0:B[C][A-1]=j[0][A]
					if A<D-1:B[C][A+1]=j[0][A]
	return B