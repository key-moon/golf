def solve_3631a71a(I):
	if not I or not I[0]:return tuple()
	F,G=len(I),len(I[0]);C=[[0 if A==9 else A for A in A]for A in I];A=min(F,G);H=list(map(list,zip(*C)));B=[[max(C[B][A],H[B][A])for A in range(A)]for B in range(A)]
	if A<=2:return tuple(map(tuple,B))
	J=[B[2:A][::-1]for B in B[1:A]];D=[A[:]for A in B]
	for(K,L)in enumerate(J,start=1):
		for(M,E)in enumerate(L,start=2):
			if E!=0:D[K][M]=E
	return tuple(map(tuple,D))
def p(g):
	A=tuple(tuple(A)for A in g)
	for C in range(256):
		B=solve_3631a71a(A)
		if B==A:break
		A=B
	return[list(A)for A in A]