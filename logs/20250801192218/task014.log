Here’s a very concise solution.  It (1) finds the first all‐zero horizontal band, (2) the first all‐zero vertical band, (3) computes which “new” color appears only in the bottom half, (4) checks whether that color lives in the bottom‐left or bottom‐right quadrant, and (5) slices out exactly that quadrant below the horizontal band:

```python
def p(g):
    h,w=len(g),len(g[0])
    # find zero‐rows
    zr=[i for i,row in enumerate(g) if row.count(0)==w]
    r0,r1=zr[0],zr[-1]
    # find zero‐cols
    zc=[j for j in range(w) if all(g[i][j]==0 for i in range(h))]
    c0,c1=zc[0],zc[-1]
    # colors in top vs bottom
    top={x for i in range(r0)   for x in g[i] if x}
    bot={x for i in range(r1+1,h) for x in g[i] if x}
    new=(bot-top).pop()
    # which side is it on?
    if any(g[i][j]==new for i in range(r1+1,h) for j in range(c0)):
        L,R=0,c0
    else:
        L,R=c1+1,w
    # slice out that quadrant
    return [row[L:R] for row in g[r1+1:h]]
```

Explanation of the key steps:

1. **Split rows**: find the first contiguous run of all‐zero rows and call its start/end `r0,r1`.  
2. **Split columns**: find the first contiguous run of all‐zero columns and call its start/end `c0,c1`.  
3. **Detect new color**: collect nonzero colors above the zero‐row band (`top`) and below (`bot`); the “new” color is `bot–top`.  
4. **Locate quadrant**: check whether that new color appears to the left of the zero‐column band or to the right.  
5. **Extract and return** the corresponding bottom‐left or bottom‐right slice.